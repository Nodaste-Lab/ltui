{
  "name": "ltui",
  "version": "0.1.0",
  "description": "Token-efficient Linear CLI for AI coding agents",
  "license": "MIT",
  "homepage": "https://github.com/Nodaste-Lab/ltui#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nodaste-Lab/ltui.git"
  },
  "bin": {
    "ltui": "bin/ltui"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/**/*.test.js",
    "prepack": "npm run build"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "SPEC.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@linear/sdk": "^73.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.0"
  }
}
